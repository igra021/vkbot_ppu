# 🤖 VK Chat Bot — консультант по утеплению домов

ВК-бот для автоматизации консультаций по утеплению домов напылением ППУ. Использует OpenAI для генерации ответов и RAG для поиска по базе знаний.

## 📋 Оглавление

- [Возможности](#-возможности)
- [Стек технологий](#-стек-технологий)
- [Установка и запуск](#-установка-и-запуск)
- [Конфигурация](#-конфигурация)
- [Структура проекта](#-структура-проекта)
- [Архитектура](#-архитектура)
- [Команды бота](#-команды-бота)
- [Разработка](#-разработка)
- [Лицензия](#-лицензия)

## 🚀 Возможности

- ✅ **Интеллектуальное общение** — использует OpenAI GPT для генерации ответов
- ✅ **Хранение истории диалогов** — каждый пользователь имеет свою историю в SQLite
- ✅ **RAG-поиск** — поиск по базе знаний (вопросы/ответы экспертов)
- ✅ **Администрирование** — команды для управления ботом (только для админа)
- ✅ **Обработка вложений** — поддерживает фото, видео
- ✅ **Пересылка медиа** — автоматическая пересылка фото/видео администратору
- ✅ **Гибкая конфигурация** — настройки через `.env` файл
- ✅ **Логирование** — подробные логи с `loguru`

## 🛠 Стек технологий

| Компонент | Технология |
|-----------|------------|
| **Фреймворк для VK API** | [vkbottle](https://github.com/vkbottle/vkbottle) |
| **LLM** | OpenAI API (Chat Completions) |
| **Язык** | Python 3.10+ |
| **База данных** | SQLite (aiosqlite) |
| **Эмбеддинги** | sentence-transformers |
| **Логирование** | loguru |
| **Управление переменными** | python-dotenv |

## Установка и запуск

### установка
pip install -r requirements.txt
### запуск
python main.py

### Клонирование репозитория

```bash
git clone https://github.com/igra021/vk-bot.git
cd vk-bot


## Конфигурация

Переменные окружения
Переменная	Описание	Обязательно
VK_TOKEN	Токен доступа к VK API	✅
ADMIN_ID	VK ID администратора	✅
OPENAI_API_KEY	API ключ OpenAI	✅
OPENAI_MODEL	Модель OpenAI (по умолчанию gpt-4o-mini)	❌
DB_PATH	Путь к файлу БД (по умолчанию conversations.db)	❌
DEBUG	Режим отладки (True/False)


## Структура проекта

vk-bot/
├── main.py                 # Точка входа
├── config.py               # Конфигурация из .env
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения (не в Git!)
├── .gitignore
├── README.md
│
├── handlers/               # Обработчики сообщений
│   ├── __init__.py         # глобальный лейблер для хендлеров
│   ├── chat.py             # хендлер для чата
│   └── admin.py            # Админ-команды
│
├── llm/                    # Работа с LLM
│   ├── __init__.py
│   ├── chat_gpt.py         # OpenAI Chat Completions
│   ├── rag.py              # RAG-поиск
│   ├── prompts.py          # Системные промпты
│   └── text_cleaner.py     # Очистка текста
│
├── db/                     # Работа с БД
│   ├── __init__.py
│   └── database.py         # SQLite операции
│   └── conversations.db    # БД
│
│
└── data/                   # Данные
    └── rag.xlsx        # База знаний для RAG


## Архитектура

┌─────────────────────────────────────────────────────────────┐
│                      VK Community                          │
│                  (Сообщения пользователей)                  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                        vkbottle                             │
│                  (Обработка событий)                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                      Handlers                               │
│           ┌────────────┴────────────┐                      │
│           │                         │                      │
│      Chat Handler             Admin Handler                │
│   (Основная логика)         (Команды /halt)               │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    LLM Layer (OpenAI)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   История   │  │   RAG       │  │  Chat Completions   │ │
│  │  (SQLite)   │  │ (Эмбеддинги)│  │    (API запрос)     │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘


## Алгоритм:
Аналитик выдает заключение, кому действовать . 
Сообщения клиента уходят в ЛЛМ. Ответ возвращается клиенту.

Если это агент-консультант, то: 
1. вопрос клиента передается в РАГ, 
2. вопрос записывается в историю. 
3. Ответ из РАГ анализируется ЛЛМ с учетом истории диалога.
4. Ответ из ЛЛМ передается клиенту, 
5. и записывается в историю.



# Что сделать

1. Доделать пересылку вложений админу
2. Админ даёт оценку вложениям. Сообщения админа передать в ЛЛМ как assistant. Как бот будет реагировать на сообщения админа?
3. Формат базы РАГ - ексель
4. базу знаний из промта перенести в РАГ с типом база знаний, цена, преимущества и т.д?

